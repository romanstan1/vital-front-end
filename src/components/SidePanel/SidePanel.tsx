import styled from "styled-components";

const Wrapper = styled.div`
  width: 200px;
  height: 100%;
  background: grey;
`;

const SidePanel = () => {
  return <Wrapper>SidePanel</Wrapper>;
};

export default SidePanel;
